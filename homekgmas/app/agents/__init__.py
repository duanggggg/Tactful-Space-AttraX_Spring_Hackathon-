"""Agent packages.

The concrete fusion implementations live under ``app.agents.fusion``.
Web-mode placeholders and future customizations live under ``app.agents.web``.
The top-level modules remain as compatibility shims.
"""
